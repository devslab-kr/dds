node_modules/
packages/*/dist/
storybook-static/
test-results/
playwright-report/
.wrangler/
dist-worker/
*storybook.log
*.log

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
